import React from 'react';
import InnerNavigation from '../InnerNavigation/InnerNavigation';

const steam = () => {
   return (
      <div className = "container">
         <div className = "container__title">steam</div>
         <InnerNavigation />
      </div>
   );
};

export default steam; 